# Database connection string (required for Neon PostgreSQL)
DATABASE_URL=postgres://user:password@db.example.neon.tech/dbname?sslmode=require

# Admin password for the admin panel
ADMIN_PASSWORD=your_secure_password

# Secret key for cookies (optional but recommended for production)
SECRET_KEY=random_secure_string